generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// Phase 1: scaffold only. The off-chain models (Gate, Registrant, Snapshot,
// SnapshotLeaf) are added in Phase 4 with the snapshot service and API.
