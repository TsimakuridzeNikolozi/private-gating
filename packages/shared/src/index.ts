// @private-gating/shared
//
// Phase 1: scaffold only. The cryptographic core (identity, Poseidon Merkle
// tree, field/byte encoding) lands in Phase 2 as identity.ts, merkle.ts,
// encoding.ts and poseidon.ts, re-exported from here.

export const SHARED_PACKAGE = "@private-gating/shared";
